dependencies {
    compileOnly asm
    compileOnly project(':api')
}

jar {
    exclude('sun', 'jdk')
}

sourceSets {
    main {
        java {
            srcDirs 'src/main/java', 'src/main/compile'
        }
    }
}

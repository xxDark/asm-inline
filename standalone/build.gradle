apply plugin: 'maven-publish'

dependencies {
    shade asm
    shade project(':api')
    shade project(':common')
}

jar {
    manifest {
        attributes 'Main-Class': 'dev.xdark.asminline.AsmInline'
    }
    from {
        configurations.shade.collect { zipTree(it) }
    }
}
